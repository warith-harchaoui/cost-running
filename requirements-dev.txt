# Contributor, test, and CI installation entry point.
#
# Installs the same runtime dependencies exercised in production, then adds the
# tools contributors and CI need. One command gives a complete environment:
#   python -m pip install -r requirements-dev.txt

# Runtime dependencies (this project, from pyproject.toml).
-r requirements.txt

# Every optional delivery surface, so the full test suite can exercise them.
fastapi>=0.110
uvicorn>=0.29
fastapi-mcp>=0.1

# Test runner and coverage reporting.
pytest>=8.2,<9
pytest-cov>=5,<7

# Formatting, linting, static analysis (PEP 8 + NumPy docstrings, CI-gated).
ruff>=0.9,<1

# HTTP client used by the API/MCP adapter tests.
httpx>=0.27
